# セクション区切りコメント

設定ファイル内のセクション区切りには以下の形式を使う:

```
# ─── セクション名 ──────────────────────────────────────
```

- `#` の後にスペース、`───` 3つ以上、スペース、セクション名、スペース、残りを `─` で埋める
- 行の長さは既存セクションに合わせる（概ね60文字程度）
- zshrc, gitconfig, starship.toml 等すべての設定ファイルで統一する
